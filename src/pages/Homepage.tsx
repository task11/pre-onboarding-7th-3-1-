import { SearchForm, SearchInput, SearchTitle } from '../feature/Search';

export default function Homepage() {
  return (
    <SearchForm>
      <SearchTitle />
      <SearchInput />
    </SearchForm>
  );
}
